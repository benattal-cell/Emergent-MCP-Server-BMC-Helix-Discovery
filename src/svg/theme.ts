export const theme = { font: "Arial, Helvetica, sans-serif", bg: "#ffffff", panelBg: "#f7f9fc", border: "#dde3ed", text: "#1d2330", textMuted: "#5a6478", accent: "#1f6feb", accentSoft: "#dbe9ff", good: "#16a34a", warn: "#d97706", danger: "#dc2626", dangerSoft: "#fee2e2", fsTitle: 22, fsHeading: 16, fsBody: 13, fsSmall: 11, pad: 16, gap: 8, radius: 8 } as const;
export function lifecycleColor(status: string | undefined): string { if (!status) return theme.textMuted; const s = status.toLowerCase(); if (s.includes("exceeded")) return theme.danger; if (s.includes("less than")) return theme.warn; if (s.includes("more than")) return theme.good; return theme.textMuted; }
