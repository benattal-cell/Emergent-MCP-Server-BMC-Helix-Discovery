import { z } from "zod"; import { DiscoveryClient } from "../discoveryClient.js"; import { renderVisual } from "../svg/renderer.js";
export const hostCardSchema = z.object({ hostName: z.string().min(1) }).strict();
export function hostCardTools(client: DiscoveryClient) { return { discovery_host_card: { description: "Render a visual ID card for a host", schema: hostCardSchema, handler: async (input: z.infer<typeof hostCardSchema>) => { const h = await client.findHosts({ nameContains: input.hostName, limit: 1 }) as any; const host = h.rows?.[0] ?? { name: input.hostName, os: "n/a", type: "n/a", key: "n/a" }; const svg = `<svg xmlns='http://www.w3.org/2000/svg' width='700' height='200'><text x='20' y='40'>${host.name}</text><text x='20' y='70'>${host.os}</text></svg>`; return renderVisual(svg, { name: `host_card_${String(host.name).replace(/[^a-z0-9_-]+/gi,"_").toLowerCase()}`, textSummary: `Host: ${host.name}` }); }, isVisual: true as const } }; }
